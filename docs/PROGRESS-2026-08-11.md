# OpenModelPool 演进进度 · 2026-08-11

> 批次：小步快跑（不推送）。本批次处理 2 个低风险加法项，3 个架构决策项 + 1 个条件项未动，待雷工拍板。

## 本批次完成项

- [2026-08-11] 完成 **P2-2(ii) admin UI 透明度面板**：新增 `admin-ledger.js` 消费 `/api/admin/ledger/transparency`、`/api/admin/ledger/contribution-quota`、`/api/admin/ledger/export`；`admin.html` 加透明度卡片 + 刷新/导出按钮 + footer 版本动态注入（修硬编码 v4.1 漂移）；`embed.go`/`admin_collab.go`/`routes.go` 接入；`ledger_panel_wire_test.go` 3 用例锁接线 + 公益黑话禁用词护栏。build/vet/test 全绿。
- [2026-08-11] 完成 **P5-4 `startRegionSyncLoop` 空转治理**：删 stubs.go sleep-only 空转循环，新增 `region_sync.go` 本地 reconciliation（填缺口/空视图禁 prune/本节点永不禁 prune，零网络 I/O、零 DNS）；`region_sync_test.go` 7 用例。`go build/vet/test ./...` 全绿（48.5s，零 FAIL）。

## 门禁结果

- `go build ./...` EXIT=0
- `go vet ./...` EXIT=0
- `go test -count=1 ./...` EXIT=0（48.5s，零 FAIL）

## 待雷工拍板的架构/设计项（本批次刻意未动）

1. **P1-1(ii) DHT 运输层桥接 + 生产接入**：`NewDHTNode` 目前只接 `InMemoryDHTNetwork`（测试台），真实 UDP/QUIC/HTTP transport 未桥接进 `init.go`。建议作为独立里程碑（改动面大、需设计传输层与现有 federation/gossip 的边界）。
2. **P1-2b-2(iv) UDP 直连数据承载协议**：直连通道目前仅用于跳过 TCP 探测，实际转发仍走 HTTP relay；需在 UDP 上自建请求/应答协议。P2 遗留独立项。
3. **P2-1(iv) 治理执行 hook**：`admit_node`/`allow_model`/`param_change` 批准后无执行效果（实为"审计账本"）。建议：**实现执行 hook 风险最低**，可下一批先做；或把父项措辞降级为"审计账本"。倾向前者。
4. **前端"积分"措辞偏离 LAUNCH-KIT 黑名单（新发现，非 BACKLOG 原项）**：`admin.html`/`admin-network.js` 仍用"积分/贡献积分"，而 `docs/LAUNCH-KIT.md` 将"积分/代币/抽成"列为"一律不用"。`contrib_points`（`network.go` 的 `ContribNodes`/`ContribPoints`，UI 显示"贡献积分"）是 P2-3 之前的旧概念（节点自身配置值 ×1000 tokens），与 P2-3 账本式免费配额并**非简单重命名**——属跨文件改写，超出本批次低风险范围，**未静默重写**，请雷工定夺是否整体改名/废弃。

## 条件项（本次未动）

- **P3-3(iii)**：自身标注"社区明确诉求时再做"，无社区诉求，刻意不动。

## 备注

- 未触碰 git 远端（代理不 push/不打 tag），代码处于"可验收"状态，待雷工 ~22:00 验收后手动 push + tag。
- 全量测试本机偶发一次 FAIL（`.nodes/*.json.tmp: The system cannot find the path specified.`）是 Windows 杀毒持 `t.TempDir()` 句柄的宿主噪音，非代码问题，勿据此回滚；本次零 FAIL。
