# OpenModelPool 演进进度 — 2026-08-12

> 本批次由雷工逐条拍板（1=C、2=A、3=B）后落地。决策 2=A 维持 P1-1(ii)/P1-2b-2(iv) "未实现/预留"，本次不动。

- [2026-08-12] 完成 P2-1(iv) 治理执行 hook（C 方案）：批准后 `admit_node`/`allow_model` 写入加成式策展名册（持久化、重载重建），`param_change` 审计-only 不应用；`/api/governance/proposals` 暴露 effective 名册；新增 `governance_exec_test.go` 5 用例；build/vet/test 全绿（test 54.5s 零 FAIL）。
- [2026-08-12] 完成 决策3(B) 去自报灌配额 + 前端统一措辞：移除 `config.ContribPoints` 自报 `*1000` 灌配额（network_quota.go 改用账本真实已捐 token；network.go 移除字段并改 status 为 `contrib_quota`）；前端 admin.html/admin-network.js 及 network.go 同意书文案的"积分/贡献积分"统一为"公益额度·贡献记账（非货币、1:1、不可交易/不可提现）"；build/vet/test 全绿。

## 遗留待雷工决策/后续
- `docs/FEDERATION.md` 仍含"贡献积分（Contribution Credit）"旧表述，属更大的文档统一清理，未在本批次改写（避免不准确内容）；建议单独一批处理或确认是否保留为历史设计文档。
- P1-1(ii) DHT 运输层桥接、P1-2b-2(iv) UDP 数据承载协议：维持"未实现/预留"，待雷工显式触发独立里程碑。
- P3-3(iii) OpenAI `/v1/responses` 等增量：条件项，社区明确诉求才做。
