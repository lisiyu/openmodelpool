# PRD：OpenModelPool 管理员页面「一键更新版本」功能（增量 / 简单 PRD）

> 角色：产品经理 许清楚（Xu）｜类型：简单 PRD（默认格式）｜增量于现有管理员页面
> 适用对象：架构师（系统设计）、工程师（实现）、QA（测试）
> 背景：本功能为后续规划优先项之一；完成后将以 LOOP 模式继续检查并实现其他未落地功能。

---

## 1. 项目信息

| 字段 | 内容 |
|---|---|
| 语言 | 简体中文 |
| Programming Language | 后端 Go；前端 原生 JS（管理面板 `admin.html` + 配套 `admin-*.js`）；**非 React** |
| Project Name | `omp_admin_one_click_update` |
| 原始需求复述 | 在管理员页面末尾、"提交PR"按钮之后，新增一个「一键更新版本」功能按钮。要求：① 新版本检测（检测到新 release 时按钮从禁用自动切为可点击）；② 点击后所有已部署 OMP 环境（本地电脑、容器、云主机等）自动执行一键更新脚本完成更新；③ 更新过程展示执行状态（进行中/成功/失败）便于追踪。 |

### 1.1 假设边界（v1 需求基线，已由主理人给定，作为本 PRD 的确定性前提）

- **技术栈约束**：OMP 为 Go 后端项目；管理员页面为 Go 模板渲染的 HTML + 原生 JS（非 React）。每个 OMP 实例拥有独立的管理页面。
- **本节点一键更新（v1 核心）**：管理页所在实例检测 GitHub release → 下载对应平台新二进制 → 原子替换 → 重启自身（复用现有 `omp-manager.sh` / supervisor 机制，已确认脚本含 `start|stop|restart|status`）。
- **跨环境更新（v1 联邦方案）**：通过联邦（federation）或已连接的 peer 网络，向已连接对端节点下发「有新版本，请自更新」信号；各 peer 收到后各自执行自身一键更新脚本并回报状态；中心管理页聚合展示「本节点 + 各 peer」的更新进度。
- **架构原则**：不存在中心节点 SSH 进所有环境执行；而是「信号广播 + 各环境自更新 + 状态汇聚」。
- **版本检测源**：GitHub Releases（公开，无需私有 token 即可查 latest release 与版本号）；需注意 API rate limit，采用缓存 + 轮询间隔策略。
- **风险标注**：上述边界中的不确定项（peer 兼容性、轮询策略、失败回滚等）见第 4 节 待确认问题。

> 说明：以上边界为需求基线，实现侧须在此框架内设计；超出边界的设想（如中心 SSH 批量执行）不在本 PRD v1 范围。

---

## 2. 产品定义

### 2.1 产品目标（为什么要做）

多环境部署 OMP（本地电脑、容器、云主机等）时，管理员需逐台登录、下载新版本、替换二进制、重启服务，升级运维成本高、易遗漏、难追踪。本功能在管理员页面提供「一键更新版本」入口：自动检测 GitHub 新 release、一处点击触发本节点与已连接各环境自更新、并集中反馈进度，将分散的多环境升级收敛为一次可视操作，显著降低运维成本与出错概率。

| ID | 目标 | 说明 |
|---|---|---|
| G1 | **检测可达（Detectable）** | 基于 GitHub Releases 自动检测新版本，无需管理员手动比对版本号；有新版本时按钮自动可点击。 |
| G2 | **一处触发、全域自更新（One-click, Self-updating）** | 点击后本节点执行自更新；并通过已连接 peer 网络向各环境广播更新信号，由各环境自行更新（非中心 SSH）。 |
| G3 | **进度可视、可追踪（Observable）** | 集中展示本节点与每个 peer 的更新状态（进行中/成功/失败）与摘要，管理员无需逐台登录排查。 |

### 2.2 用户故事（管理员视角）

| ID | 角色 | 诉求 | 收益 |
|---|---|---|---|
| US1 | 多环境 OMP 管理员 | 我希望打开管理页面就能看到是否有新版本，并可一键更新本节点 | 不必再去 GitHub 比对版本、手动下载替换重启 |
| US2 | 多环境 OMP 管理员 | 我希望点击一次就能让所有已连接环境（容器/云主机）一起更新 | 不用逐台登录执行更新脚本，省时且不易遗漏 |
| US3 | 多环境 OMP 管理员 | 我希望在管理页看到每个环境的更新进度与成败 | 更新后能快速定位失败节点，及时处置 |
| US4 | （P2 可选）运维负责人 | 我希望更新结果能通知到企业微信/邮件 | 无需一直盯着页面也能获知结果 |

---

## 3. 技术规范

### 3.1 需求池（Requirements Pool）

> 优先级：P0=必须（Must）/ P1=应该（Should）/ P2=可选（Nice-to-have）
> 范围说明：覆盖原始需求 3 点 + 第 1.1 节假设边界。

#### P0（必须）

| ID | 需求 | 验收标准 |
|---|---|---|
| P0-1 | **新版本检测 + 按钮态自动切换** | 管理页加载后（及按轮询间隔）向后端查询 GitHub latest release 版本号，与当前运行版本比较。① 当 latest > 当前：按钮由「禁用」自动切为「可点击」，并展示「发现新版本 vX.Y.Z」提示；② 当无新版本：按钮保持「禁用」态并提示「已是最新 vX.Y.Z」。检测须缓存结果并受轮询间隔约束，避免频繁打 GitHub API（rate limit）。 |
| P0-2 | **点击触发本节点一键更新** | 点击按钮后，本节点执行：检测 → 下载对应平台新二进制 → 原子替换（先写临时文件再替换，失败不影响旧版本） → 复用现有 `omp-manager.sh`/supervisor 重启自身。流程对管理员透明，无需手动干预；更新后服务自动恢复，版本号变为新版本。 |
| P0-3 | **更新状态反馈（进行中/成功/失败）** | 点击后页面展开状态反馈区，本节点一行展示状态徽标：进行中（黄/旋转）、成功（绿）、失败（红），并附进度/日志摘要（如「下载 45%」「替换完成」「重启中」）。状态须实时刷新，失败项给出失败原因摘要。 |

#### P1（应该）

| ID | 需求 | 验收标准 |
|---|---|---|
| P1-1 | **跨环境（peer）广播更新信号** | 点击更新后，除本节点外，通过联邦/已连接 peer 网络向各已连接对端下发「有新版本 vX.Y.Z，请自更新」信号；广播范围以当前已连接 peer 为准（不含离线/未连接节点，除非 Q1 另有决定）。 |
| P1-2 | **各 peer 自更新 + 状态回报** | 收到更新信号的 peer 自行执行自身一键更新脚本（同 P0-2 逻辑）；执行后向中心节点回报更新状态（进行中/成功/失败）与版本。peer 若暂不支持该信号（旧版本）应被识别为「不支持/跳过」而非卡死。 |
| P1-3 | **中心状态汇聚展示** | 中心管理页状态反馈区聚合展示「本节点 + 各 peer」的更新列表，每项含环境名/节点标识、状态徽标、进度/日志摘要。管理员可一眼看到全域更新全景与失败项。 |

#### P2（可选 / 探索性）

| ID | 需求 | 说明 |
|---|---|---|
| P2-1 | **失败重试 / 回滚提示** | 更新失败提供「重试」入口（重新执行本节点/该 peer 更新）；并提示回滚方式（如旧二进制备份路径）。是否自动回滚见 Q3。 |
| P2-2 | **更新历史记录** | 记录每次一键更新的时间、目标版本、各环境结果，便于审计与回溯。 |
| P2-3 | **企业微信 / 邮件通知** | 更新完成（成功/失败）后推送通知到企业微信或邮件（可选渠道）。 |

### 3.2 UI 设计稿（管理员页面末尾区域）

> 位置：在现有「项目介绍 & GitHub」区（含 `GitHub 仓库` / `问题反馈` / `提交 PR` 三个链接，见 `admin.html:853`）之后，新增一个独立卡片「🔄 版本更新」。原「提交 PR」按钮保持不动，新按钮在其后。

#### A. 默认态（无新版本 / 按钮禁用）

```
┌─ 🔄 版本更新 ─────────────────────────────────┐
│  当前版本：v4.1.7   [✅ 已是最新]               │
│  [ 一键更新版本 ]   ← 禁用态（灰、不可点）        │
│  提示：未检测到新版本，或检测中…                  │
└────────────────────────────────────────────────┘
```

- 禁用态按钮样式：`background:var(--bg-secondary); color:var(--text-muted); cursor:not-allowed; opacity:.6`（复用 `btn` 基础类 + 禁用属性）。

#### B. 检测到新版本（按钮可点击）

```
┌─ 🔄 版本更新 ─────────────────────────────────┐
│  当前版本：v4.1.7  →  最新版本：v4.2.0  [🟢 有更新]│
│  [🔄 一键更新版本]   ← 可点击态（主色高亮）       │
│  提示：发现新版本 v4.2.0，点击更新本节点及已连接环境│
└────────────────────────────────────────────────┘
```

- 可点击态按钮样式：`class="btn btn-primary"`（主色，鼠标悬停高亮），点击进入更新流程。

#### C. 点击后——状态反馈区（展开）

```
┌─ 🔄 版本更新（更新中…）────────────────────────┐
│  目标版本：v4.2.0                                │
│                                                  │
│  ┌─ 本节点（本地）──────────── [🔄 进行中] ──┐  │
│  │  下载 62% → 替换中…                         │  │
│  └────────────────────────────────────────────┘  │
│  ┌─ peer: omp-cloud-01 ────── [✅ 成功] ──────┐  │
│  │  已更新至 v4.2.0                            │  │
│  └────────────────────────────────────────────┘  │
│  ┌─ peer: omp-docker-02 ───── [❌ 失败] ──────┐  │
│  │  原因：下载超时（日志摘要…）[重试]          │  │
│  └────────────────────────────────────────────┘  │
│  ┌─ peer: omp-old-node ───── [⚪ 不支持] ─────┐  │
│  │  该节点版本过低，未响应更新信号             │  │
│  └────────────────────────────────────────────┘  │
│                                                  │
│  汇总：1 成功 / 1 失败 / 1 进行中 / 1 不支持      │
└────────────────────────────────────────────────┘
```

- 状态徽标配色：进行中=黄（🔄/旋转）、成功=绿（✅）、失败=红（❌）、不支持/跳过=灰（⚪）。
- 每项附「进度/日志摘要」一行；失败项附「重试」按钮（P2-1）。
- 顶部汇总行实时统计各状态数量。

HTML 结构草案（仅示意层级，非实现代码）：
```html
<div class="update-card">
  <div>当前版本 <span id="curVer">v4.1.7</span> → 最新 <span id="latestVer">v4.2.0</span></div>
  <button id="oneClickUpdateBtn" class="btn btn-primary" disabled>🔄 一键更新版本</button>
  <div id="updateStatusArea" hidden>
    <!-- 每行: 环境名 + 状态徽标 + 进度/日志摘要 + (失败时的重试) -->
    <div class="env-row" data-env="local">本节点 <span class="badge">进行中</span> <span class="log">…</span></div>
    <div class="env-row" data-env="peer">…</div>
    <div id="updateSummary">汇总：…</div>
  </div>
</div>
```

---

## 4. 待确认问题（Open Questions）

| # | 问题 | 风险 / 影响 |
|---|---|---|
| Q1 | 跨环境更新是否**仅限联邦内已连接 peer**，还是也包含手动添加但未在线的节点 / 离线环境？ | 影响 P1-1 广播范围与「不支持/跳过」判定；若含离线节点需补充唤醒/排队机制，超出 v1 自更新假设。 |
| Q2 | 版本检测**轮询间隔与缓存策略**如何定（如 5/15/30 分钟？缓存多久？）？ | 影响 P0-1 的 rate limit 与「发现新版本」的时效性；间隔过长则更新滞后，过短则触发 GitHub API 限流。 |
| Q3 | 更新**失败是否自动回滚**？还是仅提示并保留手动回滚路径？ | 影响 P2-1 与 P0-2 原子替换策略；自动回滚增加复杂度与风险（回滚本身可能失败）。 |
| Q4 | peer 自更新要求**对端也运行支持该信号的版本**；旧版本 peer 的向前兼容如何处理（识别为「不支持」/ 静默忽略 / 提示升级）？ | 影响 P1-2/P1-3 的状态模型与汇总逻辑；需在信号协议中加入版本能力协商。 |
| Q5 | 跨环境广播的**更新信号是否需要鉴权/防滥用**（避免被伪造信号诱导更新）？ | 影响 P1-1 安全设计；伪造信号可能导致拒绝服务或降级攻击，需复用现有联邦鉴权。 |
| Q6 | 本节点「原子替换 + 重启自身」在**容器/云主机不同部署形态**下的具体约束（如只读文件系统、无 supervisor）如何处理？ | 影响 P0-2 的通用性；部分环境可能无法自重启，需明确支持矩阵或降级提示。 |

---

## 5. 交付范围小结（给主理人转发用）

- **类型**：简单 PRD（默认格式）｜增量于现有管理员页面（在「提交PR」按钮之后）。
- **核心目标**：降低多环境 OMP 升级运维成本——自动检测 GitHub 新 release、一处点击触发本节点与已连接各环境自更新、集中反馈进度。
- **假设边界（基线）**：Go 后端 + 原生 JS 管理页；本节点「检测→下载→原子替换→复用 omp-manager.sh 重启」；跨环境走「联邦/peer 信号广播 + 各环境自更新 + 状态汇聚」；版本源为公开 GitHub Releases（需缓存+轮询防限流）。
- **P0（必须）**：P0-1 新版本检测 + 按钮禁用↔可点击自动切换；P0-2 点击触发本节点一键更新；P0-3 更新状态反馈（进行中/成功/失败）。
- **P1（应该）**：P1-1 peer 广播更新信号；P1-2 各 peer 自更新 + 状态回报；P1-3 中心状态汇聚展示。
- **P2（可选）**：P2-1 失败重试/回滚提示；P2-2 更新历史记录；P2-3 企业微信/邮件通知。
- **待确认（关键风险）**：跨环境范围是否含离线节点(Q1)、轮询/缓存策略(Q2)、失败是否自动回滚(Q3)、旧版 peer 向前兼容(Q4)、更新信号鉴权(Q5)、容器/云主机自重启约束(Q6)。
- **背景**：本功能为后续规划优先项之一；完成后将以 LOOP 模式继续检查并实现其他未落地功能。
