<!--
Thanks for sending this. Small PRs get reviewed faster than large ones.
If this PR implements a docs/BACKLOG.md item, mention which one.
-->

## What this changes

<!-- One or two sentences. What is different after this merges? -->

## Why

<!-- The problem being solved. Link an issue if there is one: Fixes #123 -->

## How it was verified

<!--
Which cases did you actually run? "Added a test that fails without the fix"
is the most convincing answer.
-->

## Checklist

- [ ] `go build ./...` passes
- [ ] `go vet ./...` passes
- [ ] `go test ./...` passes
- [ ] New behaviour has a test that would fail without this change
- [ ] Docs updated if behaviour or configuration changed
- [ ] No new direct dependency — or the PR explains why the standard library is insufficient
- [ ] Does not introduce a token, points economy, paid tier, or revenue share

## Anything reviewers should look at closely

<!-- Trade-offs you are unsure about, or parts you'd like a second opinion on. Delete if none. -->
